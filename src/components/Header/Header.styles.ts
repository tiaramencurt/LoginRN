import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  header: {
    backgroundColor: '#6236FF',  // El fondo violeta de la imagen
    paddingVertical: 15,
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
  },
  headerTitle: {
    color: '#FFFFFF',           // Letras blancas
    fontSize: 20,
    fontWeight: 'bold',
  },
});